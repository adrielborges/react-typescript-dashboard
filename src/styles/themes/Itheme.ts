interface ITheme {
  title: string;

  colors: {
    background: string;
    title: string;
    text: string;
    border_color: string;
    button_text: string;
    button_Background: string;
  };
}

export default ITheme;
